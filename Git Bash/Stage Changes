# To check for the changes made to local 
  $git status                          

# If any changes are present , To commit those changes

  $git commit -a    
      or
  $git add --all && git commit -m "comment"         
